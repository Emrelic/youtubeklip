services:
  - type: web
    name: youtubeklip
    runtime: node
    plan: free
    buildCommand: npm install && node setup.js
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
