services:
  - type: web
    name: youtubeklip
    runtime: node
    plan: free
    buildCommand: npm install && pip install yt-dlp bgutil-ytdlp-pot-provider
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
